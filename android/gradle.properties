org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# AGP 8.x: legacy KGP + old DSL are the normal path (AGP 9 built-in-Kotlin flags removed)
android.newDsl=false
android.builtInKotlin=false

# Mapbox private (secret) download token for resolving com.mapbox.maps artifacts.
# Do NOT commit the real value — set the SDK_REGISTRY_TOKEN environment variable
# when building (CI + local) instead. Placeholder only.
SDK_REGISTRY_TOKEN=
